{
	"name": "newswires-shared",
	"version": "1.0.0",
	"description": "",
	"exports": {
		"./*": "./src/*.ts"
	},
	"scripts": {
		"typecheck": "tsc -noEmit",
		"test": "vitest run --project shared",
		"lint": "eslint src/** --no-error-on-unmatched-pattern --fix",
		"lint:ci": "eslint src/** --no-error-on-unmatched-pattern"
	},
	"dependencies": {},
	"devDependencies": {}
}

{
	"name": "fingerpost-queueing-lambda",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"local": "FEEDS_BUCKET_NAME=local-feeds-bucket STAGE=local tsx watch localRun.ts",
		"dev": "FEEDS_BUCKET_NAME=local-feeds-bucket STAGE=dev INGESTION_LAMBDA_QUEUE_URL=https://sqs.eu-west-1.amazonaws.com/221471511793/editorial-feeds-CODE-wires-feeds-fingerpostqueueF09A4E0A-EoBlEtmqV7Ff tsx watch localRun.ts",
		"typecheck": "tsc -noEmit",
		"build": "esbuild src/handler.ts --bundle --minify --outfile=dist/handler.js --external:@aws-sdk --external:aws-sdk --platform=node",
		"lint": "eslint src/** --no-error-on-unmatched-pattern --fix",
		"lint:ci": "eslint src/** --no-error-on-unmatched-pattern"
	},
	"dependencies": {
		"newswires-shared": "*"
	},
	"devDependencies": {}
}

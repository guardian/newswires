version: '3.0'

services:
  db:
    image: postgres:16.4
    container_name: newswires-db
    ports:
      - 5432:5432
    expose:
      - 5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=newswires
